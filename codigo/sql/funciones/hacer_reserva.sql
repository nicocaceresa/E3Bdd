CREATE OR REPLACE FUNCTION 

-- se declara la función y sus argumentos
hacer_reserva_pasajero(ciudad_origen varchar, ciudad_destino varchar, fecha_salida timestamp, pasaporte_pasajero varchar)

-- se declaea lo que devuelve
RETURNS void as $$


-- se declaran las variables a utilizar en el codigo (de ser necesario)

-- se define la funcion
BEGIN
    

END